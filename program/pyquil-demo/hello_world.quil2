# hello_world program for pyQuil versions 2+
#
DECLARE ro BIT[3] # declare a 3-bit classical read-out register
X 0 # set 0th qubit to 1 (negate the 0 that it was initialized to by default
MEASURE 0 ro[2] # measure the contents of 0th qubit into the classical bit ro[2]
